# project log

** 기본 골조: ** 각 구조와 액션에 대한 기록

## File Structure

| Action | File |

## TailwindCSS

- index.css생성 bg/text설정
- 필요한 폰트 추가(index.css)

## Header와 Footer에 대한 배치와 구조 조정(CSS)

## 메인화면(Home) 배치와 구조 조정(CSS)

- bg조정 디테일하게, 창 크기에 맞춰서 사이즈 조절되게

## Ticket.tsx 구성

- dayjs를 활용하는게 타이머 제작에 유용
- 반환 값을 배열로 만들어서 쭉 뿌릴 것
- Error: 설정한 D-day는 6월 30일인데 남은 시간이 일치하지 않음(13일 남았다고 뜸)
- dayjs.duration().days()는 0~30까지만 표기되는 것 확인
- dayjs.duration().asDays()는 제한 없는 일 수 표기
- 임시로 <a>를 사용해 페이지 이동 구현

** 폰트 양식 수정 **

- @theme으로 묶어서 정리
- base에 태그 옵션 설정

### 피그마에서 필요한 이미지 다운 + dev mod 받음

- 글로벌 폰트 변경
- 피그마에서 이미지 레이어로 전부 다시 다운로드(png_layer폴더)

## Exhibitor.tsx 구성

- flex flex-col로 구성
- aspect-square / object-contain으로 이미지 구겨짐없이 구성
- 클릭 시 페이지 이동 기능 추가

## MasterClass.tsx 구성

- 각 사진을 먼저 between으로 양사이드로 몰아버리고 마지막에 중앙에 넣을 시간표를 챙기기

### tailwindcss prettier plugin적용

### header 옵션 조정

- 상단 고정 및 블러처리
- 디테일한 간격 및 위치 조정

### home.tsx 배경 옵션 변경

- 창 사이즈에 맞게 변경되지만 일정 크기 이하로는 작아지지 않도록 (max-w조정 및 vh조정)
- 글자 간격 조정
- 헤더 옵션 조정하면서 로고 위치 조정

### 부스배치도 조정

- home.tsx와 마찬가지로 max-w조정
- 글자 간격 및 사이즈 조정
- 인포 이미지 추가 및 위치 조정

### 창 사이즈에 맞춰 변경사항 수정

- 모바일 환경인 가로폭 375px로 고정하고 각 위치 수정
- 배경이랑 로고가 조금 오른쪽으로 치우쳐있는데, 다른 요소들 때문인지 아니면 옵션 설정 때문인지 파악 불가.

### 모바일 관련 페이지 분리

- 카루셀같은 경우엔 아예 페이지를 새로 만드는게 나을 것으로 판단.
- 헤더 토글 클릭 시 모바일 답게 오른쪽에서 전체(혹은 80%정도) 가리는 탭을 띄우고 거기에 세로로 나열되는 형식으로 변경할 것.
  => 클릭 시 발생하는 drawer형태.
- svg는 svg태그를 사용하거나 svgr을 적극적으로 사용할 것. => 기존 img태그로 사용하던 아이콘 svg태그로 변경 완료.

### shadcn 설치

- 라이브러리 설치가 아닌 코드째로 받아서 사용할 수 있는 형태.(해당 사이트에서 검색해서 추가하면 됨)

** Drawer 구현 **

- 아이콘을 토글로 사용해서 열고 닫기를 하려했으나, 동작하지 않음.
  => 다른 방법 생각하는중...
  => 아예 헤더가 동작하지 않게끔 modal을 활성화하고 내부에 아이콘을 같은 위치에 등장하게끔 추가!
  => drawer가 헤더보다 아래에 위치하게 해서 메인 로고를 가리지 않게함(클릭은 안되지만 미관상 헤더보다 아래인게 이쁨)

  ### ExhibitorMobile.tsx 구성
  - 캐러셀 구성을 어떻게 하면 좋을까.
  1. 원안대로 한다 => react-slick으로 그냥 단일 구현한다.
  2. 개인적인 생각대로 위스키내비 홈페이지 - 브랜드 탭 처럼 여러 개 보이게 한다.(3개, 중앙 강조, 양옆 항목 가려지게)
     \*\* 여러개 보이면 이뻐보이긴 할 듯? slick을 뜯어보면 원하는대로 수정할 수 있나?

=> 최종적으로 slick을 사용하지 않는 방향으로 작성

### MasterClass.tsx 구성

- Exhibitor.tsx를 구성해봤기 때문에 틀 자체는 그대로 가져옴.
- 강의의 세부 내용을 이미지 안쪽으로 추가.
- 처음엔 indexDot을 넣을 계획이었으나, 아무래도 없는 편이 보기에 깔끔해보여서 생략함.
